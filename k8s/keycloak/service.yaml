apiVersion: v1
kind: Service
metadata:
  labels:
    app: keycloak
  name: keycloak
  namespace: keycloak
spec:
  ports:
    - name: keycloak
      port: 80
      targetPort: 8080
  selector:
    app: keycloak
