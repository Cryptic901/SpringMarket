apiVersion: v1
kind: Service
metadata:
  name: debezium
  namespace: messaging
  labels:
    app: debezium
spec:
  ports:
    - name: debezium
      port: 3434
      targetPort: 8083
  selector:
    app: debezium
