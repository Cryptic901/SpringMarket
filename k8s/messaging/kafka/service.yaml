apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: messaging
  labels:
    app: kafka
spec:
  type: ClusterIP
  ports:
    - name: client
      port: 9090
      targetPort: 9090
  selector:
    app: kafka
