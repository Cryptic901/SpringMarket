apiVersion: v1
kind: Service
metadata:
  labels:
    app: cart-service
  name: cart-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8084
  selector:
    app: cart-service
  type: ClusterIP