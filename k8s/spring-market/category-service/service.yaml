apiVersion: v1
kind: Service
metadata:
  labels:
    app: category-service
  name: category-service
  namespace: spring-market
spec:
  ports:
    - name: http
      port: 8091
      targetPort: 8091
  selector:
    app: category-service
  type: ClusterIP