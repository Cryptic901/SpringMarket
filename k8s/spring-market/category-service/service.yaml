apiVersion: v1
kind: Service
metadata:
  labels:
    app: category-service
  name: category-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8091
  selector:
    app: category-service
  type: ClusterIP