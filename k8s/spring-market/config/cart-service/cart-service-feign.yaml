apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: cart-service
  name: cart-service-feign
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      feign:
        enabled: true