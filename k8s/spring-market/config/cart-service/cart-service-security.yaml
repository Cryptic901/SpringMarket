apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: cart-service
  name: cart-service-security
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      security:
        enabled: true