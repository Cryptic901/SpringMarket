apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: category-service
  name: category-service-security
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      security:
        enabled: true