apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: order-service
  name: order-service-feign
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      feign:
        enabled: true