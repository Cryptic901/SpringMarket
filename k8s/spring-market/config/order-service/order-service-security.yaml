apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: order-service
  name: order-service-security
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      security:
        enabled: true