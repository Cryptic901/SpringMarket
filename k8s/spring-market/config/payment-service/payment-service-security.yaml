apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: payment-service
  name: payment-service-security
  namespace: spring-market
data:
  application.yaml: |
    spring:
      security:
        enabled: true