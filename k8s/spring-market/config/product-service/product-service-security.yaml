apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: product-service
  name: product-service-security
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      security:
        enabled: true