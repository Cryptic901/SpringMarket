apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: review-service
  name: review-service-security
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      security:
        enabled: true