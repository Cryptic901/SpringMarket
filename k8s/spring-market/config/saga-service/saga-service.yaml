apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: saga-service
    environment: default
  name: saga-service-config
  namespace: spring-market
data:
  application.yaml: |
    server:
      port: 8090
      shutdown: graceful

    eureka:
      instance:
        hostname: saga-service
        prefer-ip-address: false