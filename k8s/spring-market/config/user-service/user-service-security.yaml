apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: user-service
  name: user-service-dev
  namespace: spring-market
data:
  application.yaml: |-
    spring:
      security:
        enabled: true