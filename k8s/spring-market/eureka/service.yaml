apiVersion: v1
kind: Service
metadata:
  labels:
    app: eureka-server
  name: eureka-server
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8761
  selector:
    app: eureka-server
  type: ClusterIP