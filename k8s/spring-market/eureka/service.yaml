apiVersion: v1
kind: Service
metadata:
  labels:
    app: eureka-server
  name: eureka-server
  namespace: spring-market
spec:
  ports:
    - name: http
      port: 8761
      targetPort: 8761
  selector:
    app: eureka-server
  type: ClusterIP