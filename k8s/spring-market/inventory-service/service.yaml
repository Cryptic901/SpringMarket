apiVersion: v1
kind: Service
metadata:
  labels:
    app: inventory-service
  name: inventory-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8087
  selector:
    app: inventory-service
  type: ClusterIP