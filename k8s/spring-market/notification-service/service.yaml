apiVersion: v1
kind: Service
metadata:
  labels:
    app: notification-service
  name: notification-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8089
  selector:
    app: notification-service
  type: ClusterIP