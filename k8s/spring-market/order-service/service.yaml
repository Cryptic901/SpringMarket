apiVersion: v1
kind: Service
metadata:
  labels:
    app: order-service
  name: order-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8082
  selector:
    app: order-service
  type: ClusterIP