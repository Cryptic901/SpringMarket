apiVersion: v1
kind: Service
metadata:
  labels:
    app: order-service
  name: order-service
  namespace: spring-market
spec:
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  selector:
    app: order-service
  type: ClusterIP