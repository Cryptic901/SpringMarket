apiVersion: v1
kind: Service
metadata:
  labels:
    app: payment-service
  name: payment-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8088
  selector:
    app: payment-service
  type: ClusterIP