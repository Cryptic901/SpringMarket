apiVersion: v1
kind: Service
metadata:
  labels:
    app: product-service
  name: product-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8081
  selector:
    app: product-service
  type: ClusterIP