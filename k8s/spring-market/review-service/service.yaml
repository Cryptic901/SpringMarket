apiVersion: v1
kind: Service
metadata:
  labels:
    app: review-service
  name: review-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8083
  selector:
    app: review-service
  type: ClusterIP