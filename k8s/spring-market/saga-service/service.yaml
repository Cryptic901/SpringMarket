apiVersion: v1
kind: Service
metadata:
  labels:
    app: saga-service
  name: saga-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8090
  selector:
    app: saga-service
  type: ClusterIP