apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: analytic-service
  name: analytic-service-security
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
      security:
        enabled: true
