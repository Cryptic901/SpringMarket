apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: category-service
  name: category-service
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    server:
      port: {{ index .Values.spring.services.categoryService.port }}
      shutdown: graceful

    springdoc:
      api-docs:
        path: /category-service/v3/api-docs
      swagger-ui:
        path: /category-service/swagger-ui.html