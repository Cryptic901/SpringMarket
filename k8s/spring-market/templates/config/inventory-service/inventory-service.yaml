apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: inventory-service
  name: inventory-service
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    server:
      port: {{ index .Values.spring.services.inventoryService.port }}
      shutdown: graceful

    springdoc:
      api-docs:
        path: /inventory-service/v3/api-docs
      swagger-ui:
        path: /inventory-service/swagger-ui.html