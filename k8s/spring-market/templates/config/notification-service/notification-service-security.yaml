apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: notification-service
  name: notification-service-security
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |
    spring:
     security:
       enabled: true