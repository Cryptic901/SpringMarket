apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: order-service
  name: order-service-cache
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
      cache:
        enabled: true
      data:
        redis:
          host: redis.database.svc.cluster.local
          port: 6379