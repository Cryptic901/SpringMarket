apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: order-service
  name: order-service-feign
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
      feign:
        enabled: true