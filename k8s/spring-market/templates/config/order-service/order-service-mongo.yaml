apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: order-service
  name: order-service-mongo
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
      mongo:
        enabled: true
      data:
        mongodb:
          uri: mongodb://mongo.database.svc.cluster.local:27017/orderDB
