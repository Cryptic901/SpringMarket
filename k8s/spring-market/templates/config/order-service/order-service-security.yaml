apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: order-service
  name: order-service-security
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
      security:
        enabled: true