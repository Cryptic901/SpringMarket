apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: payment-service
  name: payment-service-security
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |
    spring:
      security:
        enabled: true