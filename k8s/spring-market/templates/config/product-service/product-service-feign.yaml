apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: product-service
  name: product-service-feign
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
     feign:
       enabled: true