apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: review-service
  name: review-service-feign
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
      feign:
        enabled: true