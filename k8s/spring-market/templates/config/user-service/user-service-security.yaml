apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: user-service
  name: user-service-security
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    spring:
      security:
        enabled: true