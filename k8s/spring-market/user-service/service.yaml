apiVersion: v1
kind: Service
metadata:
  labels:
    app: user-service
  name: user-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8086
  selector:
    app: user-service
  type: ClusterIP