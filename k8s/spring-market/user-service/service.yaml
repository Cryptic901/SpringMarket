apiVersion: v1
kind: Service
metadata:
  labels:
    app: user-service
  name: user-service
  namespace: spring-market
spec:
  ports:
    - name: http
      port: 8086
      targetPort: 8086
  selector:
    app: user-service
  type: ClusterIP