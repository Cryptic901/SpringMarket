spring:
  application:
    name: EurekaServer
  cloud:
    config:
      username: config
      password: config
server:
  port: 8761
  shutdown: graceful
eureka:
  instance:
    hostname: eureka-server
    prefer-ip-address: false
  client:
    register-with-eureka: false
    fetch-registry: false
