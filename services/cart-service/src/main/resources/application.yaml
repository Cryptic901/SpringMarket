spring:
  config:
    import: "optional:kubernetes:"
  application:
    name: cart-service
  profiles:
    group:
      dev: "security,kafka,cache,feign,jpa,mongo"
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        include-profile-specific-sources: false
        enabled: true
        sources:
          - namespace: spring-market
            name: common-config-dev

          - namespace: spring-market
            name: cart-service-dev

          - namespace: spring-market
            name: cart-service-cache

          - namespace: spring-market
            name: cart-service-kafka

          - namespace: spring-market
            name: cart-service-security

          - namespace: spring-market
            name: cart-service-feign

          - namespace: spring-market
            name: cart-service-jpa

          - namespace: spring-market
            name: cart-service-mongo