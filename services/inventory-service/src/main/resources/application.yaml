spring:
  config:
    import: "optional:kubernetes:"
  application:
    name: inventory-service
  profiles:
    group:
      dev: "security,kafka,cache,jpa"
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        enabled: true
        sources:
          - namespace: spring-market
            name: common-config-dev

          - namespace: spring-market
            name: inventory-service-dev

          - namespace: spring-market
            name: inventory-service-cache

          - namespace: spring-market
            name: inventory-service-kafka

          - namespace: spring-market
            name: inventory-service-security

          - namespace: spring-market
            name: inventory-service-jpa
