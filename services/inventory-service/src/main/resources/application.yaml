spring:
  config:
    import: "optional:kubernetes:"
  application:
    name: inventory-service
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        enabled: true
        sources:
          - namespace: spring-market
            name: common-config

          - namespace: spring-market
            name: inventory-service

          - namespace: spring-market
            name: inventory-service-cache

          - namespace: spring-market
            name: inventory-service-kafka

          - namespace: spring-market
            name: inventory-service-security

          - namespace: spring-market
            name: inventory-service-jpa
