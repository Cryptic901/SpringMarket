spring:
  config:
    import: "optional:kubernetes:"
  application:
    name: review-service
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        enabled: true
        include-profile-specific-sources: false
        sources:
          - namespace: spring-market
            name: common-config

          - namespace: spring-market
            name: review-service

          - namespace: spring-market
            name: review-service-cache

          - namespace: spring-market
            name: review-service-kafka

          - namespace: spring-market
            name: review-service-security

          - namespace: spring-market
            name: review-service-feign

          - namespace: spring-market
            name: review-service-jpa

          - namespace: spring-market
            name: review-service-mongo
