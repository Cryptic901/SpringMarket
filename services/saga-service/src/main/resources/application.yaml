spring:
  config:
    import: "optional:kubernetes:"
  application:
    name: saga-service
  profiles:
    group:
      dev: "security,kafka"
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        enabled: true
        sources:
          - namespace: spring-market
            name: common-config-dev

          - namespace: spring-market
            name: saga-service-dev

          - namespace: spring-market
            name: saga-service-kafka

          - namespace: spring-market
            name: saga-service-security

