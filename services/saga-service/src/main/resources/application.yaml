spring:
  config:
    import: "optional:kubernetes:"
  application:
    name: saga-service
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        enabled: true
        sources:
          - namespace: spring-market
            name: common-config

          - namespace: spring-market
            name: saga-service

          - namespace: spring-market
            name: saga-service-kafka

          - namespace: spring-market
            name: saga-service-security

